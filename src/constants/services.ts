export const FluxoService = {
  URL: `http://${process.env.SAU_GATEWAY_HOST}/notification/api/v1/fluxo/send`
}

export const AuthService = {
  URL_LOAD_USUARIO: `http://${process.env.SAU_GATEWAY_HOST}/auth/api/usuario/`
}
